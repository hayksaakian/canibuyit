<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class='well'>
    <p>If you want availability <strong>outside the US</strong>, provide a proxy ip address and a proxy port. 
    </p>
    <p>
      <small>Otherwise availability may be innacurate.</small>
    </p>
    <div class="field">
      <%= f.label :proxy_ip %><br />
      <%= f.text_field :proxy_ip %>
    </div>
    <div class="field">
      <%= f.label :proxy_port %><br />
      <%= f.text_field :proxy_port %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
